package frontend.pages;

import frontend.abstracts.MainFrame;
import org.openqa.selenium.WebDriver;

public class EntryPoint extends MainFrame {
    public EntryPoint(WebDriver driver) {
        super(driver);
    }
}
